// --- package.json ---
// Project dependencies

{
  "name": "select-start-api",
  "version": "1.0.0",
  "description": "API for Select Start Gaming Discord Bot Data",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "mongoose": "^8.0.3"
  },
  "devDependencies": {
    "vercel": "^33.5.1"
  }
}
